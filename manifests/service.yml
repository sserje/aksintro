apiVersion: v1
kind: Service
metadata:
    name: "uksqqbe002aks01-073a"
    labels:
        app: "uksqqbe002aks01-073a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "uksqqbe002aks01-073a"
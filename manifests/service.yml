apiVersion: v1
kind: Service
metadata:
    name: "ctpsserjeaks01-59b2"
    labels:
        app: "ctpsserjeaks01-59b2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ctpsserjeaks01-59b2"